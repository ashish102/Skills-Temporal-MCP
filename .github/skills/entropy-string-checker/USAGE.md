# Usage
Run the script in this folder to compute entropy:
- `python check_string_entropy.py "<string>"`
- `python check_string_entropy.py "<string>" --threshold 3.5`

Behavior notes:
- If a threshold is not provided, the default threshold is 3.5.
- If multiple strings are provided by the user, evaluate each string separately.
